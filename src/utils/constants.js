// UTIL: constants
// This stores fixed values used across the whole app —
// like the list of supported currencies, maximum file upload size,
// and transaction status labels.
// Changing a value here updates it everywhere in the app.
